'use strict';

var typed = new Typed(".element", {
    // Waits 1000ms after typing "First"
    strings: [""],
    typeSpeed: 25,
    backSpeed: 25,
    backDelay: 1000,
    startDelay: 500,
    loop: true,
    smartBackspace: true,
    showCursor: false,
    autoInsertCss: true
  });
  